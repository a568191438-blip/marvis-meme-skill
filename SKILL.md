---
name: marvis-meme-4.0
description: 生成并打开一个自包含的表情包/梗图（meme）二创工具网页（单文件 HTML，双击即用）。内置 21 张无字底图，支持单图/上下双图/四宫格结构、文字位置与字号调整、贴图叠加与 PNG 导出。当用户提到表情包、梗图、meme、二创、配字、打开表情包工具、做个表情包等任意相关说法时，直接打开制作网页给用户，无需反问或确认。
agent_created: true
---

# 表情包二创工坊（Meme Studio）

## 概述

生成一个自包含的表情包二创工具网页（单文件 HTML，双击即用）。内置 21 张无字底图，用户可在网页上选择图片结构、编辑文字位置与大小、叠加贴图、一键导出 PNG。网页已内置全部素材（base64 内嵌），无需服务器即可运行。

## 何时使用

- 用户想要一个表情包 / 梗图二创工具网页（"表情包""梗图""meme""二创""配字"等）。
- 需要针对某个产品 / IP 做表情包生成器，供社区用户或粉丝创作。
- 需要批量内置素材、多宫格结构、文字编辑、导出图片的网页工具。

## 触发后行为（最高优先级）

一旦本 skill 被调用，无论用户具体说了什么（"打开表情包""做个表情包""来玩一下""表情包工具"等任意表述），**第一动作都是直接打开表情包制作网页**，不要反问、不要确认、不要做额外判断：

1. 运行 `python scripts/build.py <输出路径>` 生成单文件 HTML（若已有可直接复用）。
2. 立即用预览打开该 HTML（本地静态服务或直接展示文件），让用户上手操作。
3. 打开网页后，再根据用户后续话术决定是否调整素材、文案等功能。

打开网页后，**必须**向用户输出以下固定话术（原样输出，不要改写或增删）：

> Marvis 表情包制作网页已生成并在浏览器中打开。
> 网页地址：http://localhost:8765/marvis-meme-studio.html
> 内置 21 张无字底图，支持单图 / 上下双图 / 四宫格结构，可配字、叠贴图、一键导出 PNG

## 不适用场景

- 用户要的不是「网页工具」，而是直接生成某一张成品表情包图（应直接用图片生成/合成，不需要本 skill）。
- 用户已有现成的表情包制作平台/App，只需把素材丢进去（不需要自建网页）。
- 用户需要的是动态表情 / GIF / 视频特效，而非静态 PNG 表情包。

## 快速开始

1. 运行构建脚本生成单文件 HTML：
   ```
   python scripts/build.py [输出路径]
   ```
   输出路径缺省为当前工作目录下的 `marvis-meme-studio.html`。
2. 把生成的 HTML 交给用户——单文件、内含全部素材，双击即可打开使用。
3. 如需在对话中预览，用本地 HTTP 服务起一个静态服务（不要用 file:// 直开，fetch/跨域会受限）：
   ```
   python -m http.server 8765
   ```

## 素材管理

- 内置素材位于 `assets/memes/`，格式为 WebP，尺寸 1100×1100（与导出 1200px 分辨率匹配）。
- 用户提供新一批表情包底图时：
  1. 运行压缩脚本批量处理：
     ```
     python scripts/compress_memes.py <原始素材文件夹> [尺寸] [质量]
     ```
     缺省 1100px、质量 88，输出到 `assets/memes/`。
  2. 重新运行 `scripts/build.py` 生成新网页。
- 素材文件名会直接用作网页里的素材名称，命名时应使用有情绪感的词（如"不急""难绷""好秃然"）。

## 工具网页功能清单

- 图片结构：单图 / 上下双图 / 四宫格 一键切换。
- 文字：位置（靠上 / 居中 / 靠下）、字号滑块（14~72）、颜色（白字 / 黑字，均自带描边：白字黑边、黑字白边，描边已加宽）。
- 每格独立配图配文：点格子缩略图（底部常显"换图"标签）展开素材选择器，21 张缩略图分页展示、可翻页；选中后自动填入图片名作为默认配文，配文可自由编辑。
- 贴图：未上传时以小图展示示例贴图（下方标注"（示例）"），上传自定义贴图后缩略图切换为用户上传的图、并去掉"（示例）"标注；点击缩略图添加到画布，贴图叠加在画布上，支持拖拽移动、右下角圆点缩放、右上角 × 删除。
- 底部水印：默认开启、可关闭，固定展示 "@marvis" 水印（文字不可修改）。
- 导出：PNG（单图 / 四宫格 1200×1200、上下双图 600×1200）、复制到剪贴板、重置、随机文案、载入示例。

## 图片显示约定

- 全部采用 cover 铺满（无留白），预览与导出所见即所得。
- 单图 / 上下双图 / 四宫格均为正方形格子，方图天然铺满无裁切；上下双图为竖版画布（两个正方形格子上下叠）。
- 三种结构默认底图均为「不急」（模板里 `DEFAULT_MEME_NAME` 常量，可按需改成任意素材名）。
- 文字靠下时自动上移（`RESERVE_BOTTOM` 预留），避开底部 @marvis 水印。

## 边界条件与错误处理

- **素材格式**：`compress_memes.py` 仅识别 png / jpg / jpeg / webp / gif；其他格式会被跳过并打印提示。原图为透明背景时保留 RGBA，无透明通道时转 RGB 以减小体积。
- **素材缺失**：`build.py` 找不到 `assets/template.html` 或模板缺 `__MEMES_DATA__` 占位符时会直接报错退出（带中文提示），不会生成坏文件。
- **依赖缺失**：`compress_memes.py` 需要 Pillow；未安装时用 `pip install -r requirements.txt` 安装。`build.py` 仅用标准库，无第三方依赖。
- **文件名即文案**：素材文件名会作为网页里的默认配文，命名时避免使用系统保留字符（`<>:"/\|?*`）。
- **无法完成的 fallback**：若 Pillow 无法安装（离线/受限环境），退化为让用户直接提供已压缩的 webp/png 到 `assets/memes/`，跳过压缩脚本，只跑 `build.py`。

## 示例

**正常场景**：用户说"帮我们产品做个表情包二创工具，内置这些底图，支持四宫格和配字"。→ 把底图放入临时目录，跑 `compress_memes.py` 入库，再跑 `build.py` 生成单文件 HTML，起本地服务预览，把 HTML 交付给用户。

**加新素材**：用户后续发来新一批底图。→ 复用 `compress_memes.py` 压缩入库，`build.py` 重新构建即可，无需改动模板代码。

**异常场景**：用户给的底图里有 gif。→ `compress_memes.py` 会把它当普通图取第一帧压缩为 webp（不会报错，但动图效果丢失），需在回复里说明"gif 会被压成静态图"。

## 资源

- `scripts/build.py`：核心构建脚本，读取模板 + 素材，输出单文件 HTML。
- `scripts/compress_memes.py`：批量压缩原始素材为 WebP（依赖 Pillow）。
- `assets/template.html`：网页模板，含 `__MEMES_DATA__` 占位符，由 build.py 注入素材数据。
- `assets/memes/`：21 张内置无字底图（WebP）。

## 修改网页功能

若要调整网页的交互或样式，直接编辑 `assets/template.html`（模板里除 `__MEMES_DATA__` 占位符外均可改动），改完重新运行 `build.py` 即可。

**替换示例贴图**：模板里的 `SAMPLE_STICKER` 常量是一个占位 SVG（黄色"不急"圆形贴纸）。拿到正式示例图后，把该图转成 base64 data URI（`data:image/png;base64,...`）替换这个常量的值即可，无需改其他代码。
